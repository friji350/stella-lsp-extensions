language core;
extend with #exceptions, #exception-type-declaration, #nested-function-declarations;

fn main(n : Nat) -> Nat {
  fn id(x : Nat) -> Nat { return x }
  exception type = Nat
  return id(0)
}

