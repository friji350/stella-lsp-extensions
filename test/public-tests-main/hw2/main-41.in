language core;

extend with #references,
            #variants,
            #top-type,
            #bottom-type,
            #structural-subtyping;

fn main(n : Nat) -> &<| a : Nat, b : Bool |> {
  return new(<| a = succ(0) |>)
}
