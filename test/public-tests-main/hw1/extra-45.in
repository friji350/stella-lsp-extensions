language core;
extend with #letrec-bindings, #predecessor;

fn main(n : Nat) -> Nat {
  return letrec f = fn(x : Nat) {
      return fn(y : Nat) {
        return if Nat::iszero(x) then 0 else f(Nat::pred(x))(y)
      }
    } in f(n)(0)
}
